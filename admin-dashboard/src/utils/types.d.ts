export interface Admin {
  _id: string;
  firstName: string;
  lastName: string;
  email: string;
}

export interface News {
  _id: string;
  title: string;
  imageUrl: string;
  description: string;
}
